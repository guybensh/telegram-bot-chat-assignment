# Dev overrides — layered on .env when ENVIRONMENT=development.
# Copy to .env.development (gitignored).

TELEGRAM_MODE=mock

# Optional: use a separate dev bot token (overrides TELEGRAM_BOT_TOKEN from .env).
# TELEGRAM_BOT_TOKEN=
